[
    {
        "name": "trim_urls", 
        "type": "boolean", 
        "initial": true, 
        "label": "Do not trim URLs in navigation bar",
        "help_text": "By default Firefox trims many URLs (hiding the http:// prefix and trailing slash /).",
        "addons": [], 
        "config": {
            "browser.urlbar.trimURLs": false
        }
    }
]
